name: flutter_getx_i18n
description: "A code generation tool for Flutter GetX internationalization that converts CSV files into type-safe Dart translation classes."
version: 0.0.2
homepage: https://github.com/LunaGao/flutter_getx_i18n

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  build: ^4.0.0
  yaml: ^3.1.3
  build_config: ^1.3.0
  csv: ^8.0.0

dev_dependencies:
  build_runner: ^2.0.0
  flutter_lints: ^6.0.0
